{
    "label": "Macros",
    "folders": {},
    "entries": {
        "Summon Celestial Aspect": {
            "name": "召唤天界相 Summon Celestial Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.XwCNmhi9qFUDqfYW';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.Item.GNSz5ksf9fux0j7u';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Entity Aspect": {
            "name": "召唤实体相 Summon Entity Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.wRsUCpiKvEUlONAx';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.whmVZjlcusY18Z2r';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Fiend Aspect": {
            "name": "召唤邪魔相 Summon Fiend Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.d0cX3r5XBLoC9Aan';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.whmVZjlcusY18Z2r';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Dragon Aspect": {
            "name": "召唤龙相 Summon Dragon Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.IdIHKlhbug40fjr8';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.fFFQHKmFs2YBbHPK';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Monitor Aspect": {
            "name": "召唤监视者相 Summon Monitor Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.wRsUCpiKvEUlONAx';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.whmVZjlcusY18Z2r';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Ooze Aspect": {
            "name": "召唤泥怪相 Summon Ooze Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.yRcCXxoHCNGSWSh0';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.Item.SlQSir9DtX0rI0lA';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Fey Aspect": {
            "name": "召唤精类相 Summon Fey Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.oD13HAUTQ6UIDdtZ';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.TUDKBMHIcyoCLs5x';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Animal Aspect": {
            "name": "召唤动物相 Summon Animal Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.2uDnYIc16jKJdPK1';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.TLIkcaSk5Zx3iFjK';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Plant Aspect": {
            "name": "召唤植物相 Summon Plant Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.VJRMPhu1NYPvY0tz';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.Item.CM5I6zG1L7Q5HamS';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Construct Aspect": {
            "name": "召唤构装相 Summon Construct Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Actor.fbm9MHSnilOoGve6';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.Item.vzxNTBV15l6LVfYM';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Undead Aspect": {
            "name": "召唤不死相 Summon Undead Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.Ea3IHrVajNxwpg9V';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.MXTfjiyueDwVyobC';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Thurse Aspect": {
            "name": "召唤巨魔相 Summon Thurse Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.h2IRWNyNrZFvZH1N';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.RhhDWeuA1RBPLy1W';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        },
        "Summon Elemental Aspect": {
            "name": "召唤元素相 Summon Elemental Aspect",
            "command": "const actorUuid = 'Compendium.pf2e-team-plus-magic.actors.bmXqQTbhXycBBnvy';\nconst effectUuid = 'Compendium.pf2e-team-plus-magic.misc.ARrJ3NLQQRzneYjc';\n\nif (!window.foundrySummons) return ui.notifications.warn(\"Foundry Summons is not installed! Cannot automate summoning without it.\");\n\nlet rank = 1;\nlet message;\nlet originItem;\nif (scope.event) {\n    const messageEl = scope.event.target.closest(\".chat-message.message\")\n    if (messageEl) {\n        const messageId = messageEl.dataset.messageId;\n        message = game.messages.get(messageId);\n        if (message) {\n            originItem = message.item;\n            rank = message.item.rank;\n            if (message.token) token = message.token;\n        }\n    }\n} else {\n  const highestRank = Math.max(...token.actor.spellcasting.collections.contents.map(x => x.highestRank));\n  const data = await foundry.applications.api.DialogV2.input({\n    window: { title: \"Pick spell rank\" },\n    content: `<select name=\"rank\">\n    ${Array.from({ length: highestRank }, (_, i) => `<option value=\"${i + 1}\">${i + 1}</option>`).join(\"\")}\n  </select>`,\n    ok: {\n      label: \"Save\",\n      icon: \"fa-solid fa-floppy-disk\",\n    }\n  })\n\n  rank = data.rank;\n}\n\nif (!token) return ui.notifications.warn(\"Select the summoning token before summoning.\");\n\nconst actorDoc = await fromUuid(actorUuid);\nconst summon = await foundrySummons.pick({ \n    actor: actorDoc, \n    updateData: {\n        system: {\n            details: {\n                level: {\n                    value: rank\n                }\n            }\n        }\n    }\n})\n\nconst effectDoc = await fromUuid(effectUuid);\nconst effect = effectDoc.toObject()\nconst spellcasting =\n  originItem?.isOfType(\"spell\") && originItem.spellcasting\n      ? {\n            attribute: {\n                type: originItem.attribute,\n                mod: originItem.spellcasting.statistic?.attributeModifier?.value ?? 0,\n            },\n            tradition: originItem.spellcasting.tradition,\n        }\n      : null;\n\neffect.system.level.value = rank;\neffect.system.context = {\n    origin: {\n        actor: token.actor.uuid,\n        token: message?.token.uuid || token.uuid,\n        item: message?.item.uuid,\n        rollOptions: message?.flags.pf2e.origin?.rollOptions ?? [],\n        spellcasting,\n    }\n};\n\ngame.actors.get(summon.actorId)?.createEmbeddedDocuments(\"Item\", [effect])"
        }
    }
}